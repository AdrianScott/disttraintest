line-length = 88
target-version = "py310"

# Rules: pycodestyle (E), pyflakes (F), isort (I)
select = ["E", "F", "I"]

# Black compatibility
extend-ignore = ["E203"]

exclude = [
  "artifacts/",
  "runs_ddp/",
  "logs/",
  "reports/",
]

